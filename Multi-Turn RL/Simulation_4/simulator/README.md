# Simulator

This package groups the **Gymnasium environment** and supporting components that define the customer-support simulation:
- environment (`SupportEnv`)
- state/transition dynamics
- reward model
- RAG context injection
- optional customer NLG

Code currently lives in `Simulation_4/env/` and `Simulation_4/rag/` and is re-exported here for cleaner imports.
