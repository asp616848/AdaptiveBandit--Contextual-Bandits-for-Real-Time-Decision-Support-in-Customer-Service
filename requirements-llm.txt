# Optional dependencies for LLM-powered (Phase 13) runs and HF model download.
# Not required for the one-command evaluation path (bash run.sh).

openai>=1.0
huggingface_hub>=0.20
transformers>=4.40
accelerate>=0.29
sentencepiece>=0.2
