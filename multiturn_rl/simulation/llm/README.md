# LLM Backends

This package contains optional language-model backends used by the **full NLP** variant:
- `ollama` via OpenAI-compatible API
- `hf-qwen` via Hugging Face model weights (downloadable)

The Docker evaluation path in `run.sh` does **not** require these backends.
