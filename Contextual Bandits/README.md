# Contextual Bandits

This folder is reserved for the contextual bandits component of the project.

For the current submission, `run.sh` runs the **Multi-Turn RL** pipelines (numerical + NLP/text-observation).
